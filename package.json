{
  "name": "sudokunonsense",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "bundle": "esbuild dist/src/entry.js --bundle --outfile=dist/bundle.js --platform=browser --minify"
  },
  "devDependencies": {
    "esbuild": "^0.18.20",
    "typescript": "^5.0.0"
  }
}
